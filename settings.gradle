rootProject.name = 'groovy-language-server'

include 'org.eclipse.groovy.ls.core'
include 'org.eclipse.groovy.ls.product'

// The VS Code extension is built separately via npm/webpack,
// not as a Gradle subproject.
